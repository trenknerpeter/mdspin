-- Record-only (applied to hosted project ixdsddfxkrkytiitfici via Supabase MCP).
--
-- Original byte size of the source file, captured at conversion time.
-- Powers exact ROI / cost-savings on the Dashboard Insights panel
-- (going-forward only). NULL for rows created before this column and for
-- URL conversions (which have no source file).
ALTER TABLE public.conversions ADD COLUMN IF NOT EXISTS source_bytes integer;
